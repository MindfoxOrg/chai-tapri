interface UserSignUp {
    email: string;
    password: string;
    name: string;
    phoneNumber: string;
    birthDate: string;
  }
  
  interface Auth {
    email: string;
    bearer: string;
  }

